// Entry point for the agent injected into service webviews.
// This is a placeholder: the real frame guard, recipe selection and
// report loop are added in later tasks (see design.md §2.2.14).
console.log("[eluma-agent] placeholder agent loaded");
